-r requirements-full.txt

# Optional live natural-language resolver. Keep the current major version
# because the upstream SDK has announced breaking AFC changes for v3.
google-genai>=2.19,<3

# Optional accelerator packages for the hosted model path. The current
# PyTorch/MPS engine does not require MLX, but benchmark utilities may.
mlx>=0.32; platform_system == "Darwin"
mlx[cuda12]>=0.32; platform_system == "Linux"
