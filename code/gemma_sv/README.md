# Gemma-SV implementation

Gemma graft, exact decrement certificate, masked-refit behavioral evaluations,
demo, and reproduction scripts for the accompanying preprint.

Run commands from the parent `code/` directory. See `../README.md` for the
quick start and `reproducibility/PROVENANCE.md` for claim-level provenance.
