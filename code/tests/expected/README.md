# Expected numerical results

These three data-only TeX macro files are frozen numerical fixtures from the
GemmaSV manuscript. The tests reconstruct them from hash-bound source-free
measurements. They are not manuscript sources and require no TeX engine.
The scientific generators remain unchanged; the standalone tests point to
this directory instead of a paper-build directory.
