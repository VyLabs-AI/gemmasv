# Third-party notices

Original replication code and release tooling are supplied under Apache-2.0.
Upstream models, datasets and dependencies retain their own licenses and terms.
The frozen study bundle preserves its included license files unchanged.

Gemma model weights, FineWeb-Edu, WikiText, TOFU, LongMemEval, RULER and optional
natural-QA benchmark inputs are not redistributed. Reproducers must obtain the
pinned upstream inputs independently under their applicable access terms.
Python numerical and model dependencies are installed separately from their
respective requirement files; they are not vendored here.

The released source-free aggregates do not include source conversation
collections, full generated responses or private human-validation packets.
Explicitly synthetic fixtures support state and certificate tests.
The current release has no TeX style package, Node figure-tool dependency tree,
hosted API or website. Historical copies remain in Git history.

The incremental/decremental solver attribution and upstream reference terms
are preserved in `code/cp_svm/THIRD_PARTY_NOTICES.md`.
